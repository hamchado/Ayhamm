import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.latakia.dentalsyndicatebranch',
  appName: 'Latakia Dental Syndicate Branch',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
